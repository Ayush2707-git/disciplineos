/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: [
    'alto-concert-retrieval-inn.trycloudflare.com', 
    '*.trycloudflare.com',
    '*.loca.lt',
    'localhost:3000'
  ]
};

export default nextConfig;
